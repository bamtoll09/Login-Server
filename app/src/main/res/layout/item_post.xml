<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="24dp"
    android:layout_margin="8dp"
    >

    <TextView
        android:id="@+id/text_post_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Title" />

    <View
        android:id="@+id/post_divider1"
        android:layout_width="1dp"
        android:layout_height="match_parent"
        android:layout_margin="4dp"
        android:background="@android:color/holo_orange_light"
        android:layout_toLeftOf="@id/text_post_writer"
        />

    <TextView
        android:id="@+id/text_post_writer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Writer"
        android:layout_toLeftOf="@id/post_divider2"
        />

    <View
        android:id="@+id/post_divider2"
        android:layout_width="1dp"
        android:layout_height="match_parent"
        android:layout_margin="4dp"
        android:background="@android:color/holo_orange_light"
        android:layout_toLeftOf="@id/text_post_date"
        />

    <TextView
        android:id="@+id/text_post_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Date"
        android:layout_alignParentRight="true"
        />

</RelativeLayout>